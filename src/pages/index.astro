---
import { getCollection } from 'astro:content';
import Base from '../layouts/base.astro';
import HomeNav from '../components/navigation/home_nav';
import Notification from '../components/utils/notification/notification';
import UpdateLatest from '../components/updates/latest';
import { Image } from 'astro:assets';
import Current from '../components/utils/notification/current';
import Heading from '../components/small/heading.astro';
import { siJavascript, siPython, siRust } from 'simple-icons';

const options = [
    'is a Developer',
    'is into ML now',
    'creates websites',
    'is a student',
];

let posts = await getCollection('blog', ({ data }) => {
    return data.draft !== true;
});

posts = posts.sort(
    (a: any, b: any) =>
        new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);
const latest = posts
    .filter(
        (post: any) => post.data.type != 'quick' && post.data.type != 'notes'
    )
    .slice(0, 10);
---

<Base title="NoobScience | Student and Open Source Enthusiast">
    <div class="px-3">
        <h1 class="text-xl">
            Hey! I am <a href="/about" class="font-bold">Ishan</a>👋
        </h1>
        <div>
            <p class="pt-4">
                A guy who found his passion for code when he was 18 and now just
                can't stop coding. Likes to code in <span
                    id="inline-icon"
                    set:html={siRust.svg}
                /> Rust for elegance and class however, gets sucked into the
                <span
                    id="inline-icon"
                    class="text-yellow-400"
                    set:html={siJavascript.svg}
                /> JS and <span
                    class="text-blue-400"
                    id="inline-icon"
                    set:html={siPython.svg}
                /> Py void. Studies Computer Science at <a
                    class="font-bold"
                    href="https://griet.ac.in">GRIET</a
                > while living in Hyderabad, India. Currently <Notification
                    client:load
                />
                <a class="ml-1" href="/now"
                    ><i class="text-blue-500 bi bi-arrow-right"></i></a
                >
            </p>
            <p class="pt-2">
                Welcome to my site. Grab a coffee and have a look around. You'll
                find a very cool <a class="font-bold" href="/blog">blog</a>,
                some <a class="font-bold" href="/projects">projects</a> and some


                <a class="font-bold" href="/tools">tools</a> if you are
                interested.
            </p>
        </div>
        <div class="md:pt-6 pt-2">
            <h2 class="text-xl pt-2 font-bold">
                <Heading level={2} /> My Latest Quip
            </h2>
            <p class="pt-2">
                Well, think of quips as tweets but on my own site. I post my
                thoughts and ideas here. More 
                <a class="font-bold" href="/quips">here</a
                >
            </p>
            <UpdateLatest client:idle />
        </div>
        <div>
            <h2 class="text-xl pt-2 font-bold">
                <Heading level={2} /> Brief about me
            </h2>
            <p class="pt-2">
                Sounds professional 😎 and for our <a
                    class="font-bold"
                    href="/blog/social-media/gold-fish">gold fish</a
                > attention spans
            </p>
            <ul id="points" class="list-none pt-2 grid grid-cols-2 gap-x-4">
                <li>🎒 1st year Student at GRIET</li>
                <li>🧑‍💻 Full Stack Developer</li>
                <li>
                    <i class="bi bi-github pl-1"></i> Open Source Enthusiast
                </li>
                <li>🌱 Web Gardener</li>
                <li>🤖 Learning to teach Machines</li>
                <li>📱 Trying to distance from Social Media</li>
                <li>🌐 Loves building and maintaining sites</li>
                <li>🐶 Dog Lover</li>
                <li></li>
            </ul>
        </div>
        <div class="pt-2">
            <h2 class="text-xl pt-2 font-bold">
                <Heading level={2} /> Fresh from the Blog
            </h2>
            <p class="pt-2">
                Take a look at my blogs. They are pretty cool. I write about
                tech, life and everything in between. More <a
                    class="font-bold"
                    href="/blog">here</a
                >. I also cross post these to every platform I am on. So, you
                can read them there too. Follow me on my <a
                    class="font-bold"
                    href="/social">socials</a
                >
            </p>
            <div class="pt-2 grid grid-cols-2 gap-x-4 gap-y-2">
                {
                    latest.map((post: any) => {
                        return (
                                <div class="py-1">
                                    <h2 class="text-xl font-heading">
                                        <a href={'/blog/' + post.slug}>
                                            {post.data.emoji}{' '}{post.data.title}
                                        </a>
                                    </h2>
                                    <p class="">{post.data.description.slice(0, 100) + "..."}</p>
                                </div>
                        );
                    })
                }
            </div>
        </div>
    </div>
</Base>
<style is:inline>
    #heading {
        line-height: 4rem;
    }
    #points li {
        padding: 0.25rem;
    }
    @media screen and (max-width: 768px) {
        #heading {
            line-height: 3rem;
        }
    }
</style>
