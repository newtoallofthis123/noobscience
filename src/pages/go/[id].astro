---
const { id } = Astro.params;

const res = await fetch(`http://${Astro.url.host}/api/go/${id}`)
if(res.status !== 200){
    return Astro.redirect('/404')
}
else{
    const data = await res.json()
    return Astro.redirect(data.url)
}
---